# Snake Game

Implementação simples do jogo da cobrinha em uma linguagem funcional.

## Uso

Com o [cabal](https://www.haskell.org/cabal/) instalado, instale as dependencias do projeto:<br>
`$ cabal install`

Execute a aplicação:<br>
`$ cabal run`
